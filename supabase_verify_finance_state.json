{
  "project_id": "dyfhikvqkqswobdqnxeq",
  "query": "select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid = c.relnamespace where n.nspname = $q$public$q$ and c.relname = $q$finance_state$q$ limit 1;"
}
